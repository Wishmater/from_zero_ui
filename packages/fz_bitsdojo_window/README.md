# fz_bitsdojo_window

> **DEPRECATED** - This package is deprecated and will be deleted once all projects using it migrate to new Flutter-basic APIs for window management.

Provides `WindowExtended` with `appWindow` access and `windowsDesktopBitsdojoWorking` flag.
