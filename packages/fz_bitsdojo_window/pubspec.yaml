name: fz_bitsdojo_window
description: Bitsdojo window integration for FromZero UI. (DEPRECATED)
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: "^3.9.0"

dependencies:
  flutter:
    sdk: flutter

  bitsdojo_window: ^0.1.6
  bitsdojo_window_platform_interface: ^0.1.2

  fz_platform:
    path: ../fz_platform

dev_dependencies:
  flutter_lints: ^5.0.0
